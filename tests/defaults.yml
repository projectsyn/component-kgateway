applications:
  - gateway-api

parameters:
  # Dependency for testing with upstream lib
  kapitan:
    dependencies:
      - type: https
        source: https://raw.githubusercontent.com/projectsyn/component-gateway-api/initial-implementation/lib/gateway-api.libsonnet
        output_path: vendor/lib/gateway-api.libsonnet
